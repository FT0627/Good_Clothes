<h3>ユーザー詳細</h3>
<div class="container">
  <div class="row mx-auto">
    <div class="col-lg-5 d-flex flex-column mb-3">
      <%= image_tag @user.get_profile_image(300,300) %>
      <div>
      フォロー・フォロワー
      </div>
      <div>
      <%= @user.name %>
      </div>
      <div>
      <%= @user.email %>
      </div>
      <div>
      <%= link_to "編集", edit_path, class: "btn btn-primary" %>
      </div>
    </div>
    <div class="col-lg-5">投稿一覧(4枚)</div>
  </div>
</div>
