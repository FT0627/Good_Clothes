<div>
  <%= flash[:notice] %>
</div>

<div class="container text-center">
  <h1 class="my-3">top</h1>
  <div class="row ">
        <% @items.each do |item| %>
        <div class="col-lg-3">
          <%= link_to item_path(item) do %>
          <%= image_tag item.item_images[0], size: "180x240" %>
          <% end %>
        </div>
        <% end %>
  </div>
</div>
