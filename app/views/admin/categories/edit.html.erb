<div class="container">
  <div class="w-50">
    <h1 class="my-3">カテゴリー</h1>
    <%= form_with model: @category, url: admin_categories_path, metod: :patch do |f| %>
      <h4 class="mb-3">カテゴリー名</h4>
      <%= f.text_field :name, autofocus: true %>
      <%= f.submit '編集' %>
    <% end %>
  </div>
</div>